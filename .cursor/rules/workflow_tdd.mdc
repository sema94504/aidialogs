---
alwaysApply: false
---
Работай строго по @docs/tasklist.md. Выполняй итерации последовательно по TDD-подходу.

## Цикл каждой итерации

### 1. ПЛАНИРОВАНИЕ
- Прочитай текущую итерацию из tasklist
- Предложи решение с конкретными отрезками кода
- Определи тесты для TDD-цикла
- Проверь соответствие принципам из docs/vision.md
- ОЖИДАЙ согласования перед началом

### 2. РЕАЛИЗАЦИЯ (TDD-цикл)

#### 2.1 RED (Красная полоса)
- Напиши падающий тест для нового функционала
- Запусти `make test` - убедись что тест падает правильно
- Зафиксируй причину и место падения
- Следуй @qa_conventions.mdc
- Сообщи: 🔴 RED статус

#### 2.2 GREEN (Зеленая полоса)
- Реализуй минимальный код для прохождения теста
- Запусти `make test` - убедись что тест проходит
- НЕ оптимизируй код на этом этапе
- Следуй правилам из docs/conventions.md
- Сообщи: 🟢 GREEN статус

#### 2.3 REFACTOR
- Оптимизируй реализацию
- Запусти `make test` - тесты остаются зелеными
- Применяй инструменты качества:
  - `make format` - форматирование кода
  - `make lint` - проверка стиля
  - `make typecheck` - проверка типов
- Сообщи: ♻️ REFACTOR статус

### 3. ФИНАЛЬНАЯ ПРОВЕРКА
- Запусти `make test` - все тесты зеленые
- Запусти `make coverage` - проверь покрытие >80%
- Убедись что всё работает
- ОЖИДАЙ подтверждения

### 4. ПРОВЕРКА КАЧЕСТВА
- Запусти `make lint` - исправь все ошибки
- Запусти `make format` - отформатируй код
- Запусти `make typecheck` - проверь типы
- Проверь соответствие принципам из docs/vision.md:
  - KISS - код остался простым?
  - MVP - не добавлено лишнего?
  - 1 класс = 1 файл?
  - Линейный код, прямые зависимости?

### 5. ФИКСАЦИЯ
- Отметь выполненные задачи (✅) в tasklist
- Обнови таблицу прогресса (статус ✅, дата)
- Сделай коммит

### 6. ПЕРЕХОД
- СОГЛАСУЙ переход к следующей итерации
- Повторяй цикл

Коммиты пиши:
- Глагол в третьем лице: добавлено, исправлено, реализовано
- Максимум 80 символов
- Отвечает на вопрос "Что изменится после коммита?"
- Без точки в конце
- Примеры: "Реализовано Config и структуру проекта", "Добавлено LLMClient с поддержкой OpenAI API"

## ОБЯЗАТЕЛЬНО

- ❌ НЕ начинай реализацию без согласования
- ❌ НЕ переходи к следующей итерации без подтверждения
- ❌ НЕ пиши код до написания теста (нарушение TDD)
- ❌ НЕ оптимизируй на GREEN-фазе
- ❌ НЕ коммить код с ошибками линтера
- ❌ НЕ коммить код с падающими тестами
- ✅ Всегда начинай с RED (тест падает)
- ✅ GREEN - минимальная реализация
- ✅ REFACTOR - только с зелеными тестами
- ✅ Обновляй tasklist после каждой итерации
- ✅ Делай коммит после каждой итерации
- ✅ Запускай `make test` после каждой фазы TDD

## Инструменты качества

Используй команды:
```bash
make format     # Форматирование кода (Ruff)
make lint       # Проверка стиля (Ruff)
make typecheck  # Проверка типов (Mypy)
make test       # Запуск тестов
make coverage   # Покрытие тестами
```

Порядок в TDD:
1. RED: `make test` - тест падает
2. GREEN: `make test` - тест проходит
3. REFACTOR: `make format && make lint && make typecheck` - качество
4. Финал: `make test && make coverage` - проверка

## Структура сообщений

### При планировании:
```
Итерация N: [название]

Предлагаю следующее решение:
[описание]

TDD план:
- Тест 1: [что тестируем]
- Тест 2: [что тестируем]

Изменения:
- [файл 1]: [что меняется]
- [файл 2]: [что меняется]

Код:
[ключевые отрезки кода]

Проверка vision.md:
- [принцип 1]: [соответствует/не соответствует]
- [принцип 2]: [соответствует/не соответствует]

Готов начать после согласования.
```

### После RED:
```
🔴 RED: [название теста]

Тест: [описание теста]
Файл: [путь к файлу теста]
Ожидаемое падение: [причина падения]

make test: ❌ FAILED
```

### После GREEN:
```
🟢 GREEN: [название теста]

Реализация: [описание реализации]
Файл: [путь к файлу кода]
Минимальная реализация для прохождения теста

make test: ✅ PASSED
```

### После REFACTOR:
```
♻️ REFACTOR: [что улучшено]

Изменения: [описание улучшений]
Качество:
- make format: ✅
- make lint: ✅
- make typecheck: ✅
- make test: ✅ все тесты зеленые
```

### После итерации:
```
Итерация N: завершена

TDD цикл:
🔴 RED → 🟢 GREEN → ♻️ REFACTOR

Реализовано:
- [список выполненных задач]

Тесты: [результат make test]
Линтер: [результат make lint]
Coverage: [% покрытия]

Проверка vision.md:
✓ [принцип 1]
✓ [принцип 2]

Обновил tasklist, готов делать коммит.
```

## TDD принципы

1. **RED первым** - всегда начинай с падающего теста
2. **Минимум для GREEN** - не пиши лишнего кода
3. **Рефактор с зелеными** - оптимизируй только когда тесты проходят
4. **Один цикл = один тест** - не смешивай несколько фич в одном RED-GREEN-REFACTOR
5. **Документируй фазы** - сообщай статус после каждой фазы
